# 工作计划：修正端口文档错误

## 问题描述
项目文档中端口描述有误：
- **实际端口**：12396（代码中 MyToolWindowFactory.kt 第 32 行定义的 PORT 常量）
- **文档描述**：10086（README.md 和其他文档中记录）

## 需要修改的文件和位置

### 1. README.md（6 处修改）

| 行号 | 当前内容 | 修改后 |
|------|----------|--------|
| 15 | `4096` 改为 `10086` | `4096` 改为 `12396` |
| 82 | `127.0.0.1:10086` | `127.0.0.1:12396` |
| 84 | `--port 10086` | `--port 12396` |
| 97 | `端口: 10086` | `端口: 12396` |
| 111 | `端口 10086` | `端口 12396` |

### 2. 根目录 AGENTS.md（1 处修改）

| 行号 | 当前内容 | 修改后 |
|------|----------|--------|
| 42 | `127.0.0.1:4096` | `127.0.0.1:12396` |

## 具体修改指令

### README.md 修改

```markdown
第 15 行：
OLD: 1. **端口修改** - 将默认端口从 `4096` 改为 `10086`，避免与常见服务端口冲突
NEW: 1. **端口修改** - 将默认端口从 `4096` 改为 `12396`，避免与常见服务端口冲突

第 82 行：
OLD: 插件会自动检查 127.0.0.1:10086 端口，如果服务未运行，将自动执行：
NEW: 插件会自动检查 127.0.0.1:12396 端口，如果服务未运行，将自动执行：

第 84 行：
OLD: opencode serve --hostname 127.0.0.1 --port 10086
NEW: opencode serve --hostname 127.0.0.1 --port 12396

第 97 行：
OLD: - 端口: 10086
NEW: - 端口: 12396

第 111 行：
OLD: - 检查端口 10086 是否已被其他程序占用
NEW: - 检查端口 12396 是否已被其他程序占用
```

### 根目录 AGENTS.md 修改

```markdown
第 42 行：
OLD: - 管理 `opencode serve` 进程（127.0.0.1:4096）
NEW: - 管理 `opencode serve` 进程（127.0.0.1:12396）
```

## 验证步骤

修改完成后运行以下命令验证：

```bash
# 检查是否还有遗漏的 10086
grep -r "10086" . --include="*.md" --exclude-dir=".sisyphus" --exclude-dir="build" --exclude-dir=".git"

# 检查是否还有遗漏的 4096
grep -r "4096" . --include="*.md" --exclude-dir=".sisyphus" --exclude-dir="build" --exclude-dir=".git"

# 确认所有端口都改为 12396
grep -r "12396" . --include="*.md" --exclude-dir=".sisyphus" --exclude-dir="build" --exclude-dir=".git"
```

预期结果：
- 第一个命令应该找不到任何结果（所有 10086 已修改）
- 第二个命令应该找不到任何结果（所有 4096 已修改）
- 第三个命令应该找到 README.md 和 AGENTS.md 中的正确端口

## TODO

- [x] 1. 修改 README.md 第 15 行（端口历史说明）
- [x] 2. 修改 README.md 第 82 行（端口描述）
- [x] 3. 修改 README.md 第 84 行（命令示例）
- [x] 4. 修改 README.md 第 97 行（配置说明）
- [x] 5. 修改 README.md 第 111 行（故障排除）
- [x] 6. 修改根目录 AGENTS.md 第 42 行（实现说明）
- [x] 7. 运行验证命令确认修改完整性

## 执行说明

此计划由 Prometheus 创建。运行 `/start-work fix-port-doc` 让 Sisyphus 执行器完成修改。
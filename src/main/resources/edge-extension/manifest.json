{
  "manifest_version": 3,
  "name": "opencode-web-bootstrap",
  "version": "1.0.0",
  "description": "Bootstraps the OpenCode Web UI sidebar with the current IntelliJ project, and pre-allows browser notifications for the OpenCode server origin via a background service worker. Injected by the intellij-opencode-web plugin via Edge --load-extension; no user-facing UI.",
  "host_permissions": ["http://localhost:12396/*"],
  "permissions": ["contentSettings"],
  "background": {
    "service_worker": "background.js"
  },
  "content_scripts": [
    {
      "matches": ["http://localhost:12396/*"],
      "js": ["content.js"],
      "run_at": "document_start",
      "world": "MAIN"
    }
  ]
}

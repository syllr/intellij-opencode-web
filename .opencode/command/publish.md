# Publish Plugin to JetBrains Marketplace

## description
发布 OpenCode Web UI 插件到 JetBrains Marketplace

## args

## run
```bash
cd /Users/yutao/IdeaProjects/intellij-opencode-web && set -a && source local.properties && set +a && ./gradlew buildPlugin && ./gradlew publishPlugin
```

## env
```json
{}
```
